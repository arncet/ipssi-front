//import { USER_LOGIN_SUCCESS, USER_LOGIN_FAILED } from '../actions'

export default function layer (state = { gmail: null, contacts: null, news: null, agenda: null, docs: null }, action) {
  switch (action.type) {
    default:
      return state
  }
}
